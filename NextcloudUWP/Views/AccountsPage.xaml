<Page x:Class="NextcloudUWP.Views.AccountsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="#0082c9" Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="BackButton_Click"
                    Foreground="White" Background="Transparent">
                <SymbolIcon Symbol="Back" />
            </Button>
            <TextBlock Grid.Column="1" Text="Accounts"
                       Foreground="White" VerticalAlignment="Center"
                       FontSize="18" Margin="8,0,0,0" />
        </Grid>

        <!-- Account list -->
        <ListView x:Name="AccountsList" Grid.Row="1"
                  SelectionMode="None" IsItemClickEnabled="True"
                  ItemClick="AccountsList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4,12" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Ellipse Grid.Column="0" Width="36" Height="36"
                                 Fill="#0082c9" />
                        <TextBlock Grid.Column="0"
                                   Text="{Binding Initial}"
                                   Foreground="White" FontSize="18" FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Label}"
                                       FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ServerUrl}"
                                       FontSize="11" Foreground="#757575"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <SymbolIcon Grid.Column="2" Symbol="Accept"
                                    Foreground="#0082c9" Margin="8,0"
                                    Visibility="{Binding ActiveVisibility}" />
                        <Button Grid.Column="3" Content="Remove"
                                FontSize="11" Padding="8,4"
                                Click="RemoveAccount_Click"
                                Tag="{Binding AccountKey}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Bottom bar -->
        <CommandBar Grid.Row="2">
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Add" Label="Add Account" Click="AddAccount_Click" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</Page>
