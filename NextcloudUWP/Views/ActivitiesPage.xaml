<Page x:Class="NextcloudUWP.Views.ActivitiesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Background="#0082c9" Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="BackButton_Click"
                    Foreground="White" Background="Transparent">
                <SymbolIcon Symbol="Back" />
            </Button>
            <TextBlock Grid.Column="1" Text="Activity"
                       Foreground="White" VerticalAlignment="Center"
                       FontSize="18" Margin="8,0,0,0" />
        </Grid>

        <Grid Grid.Row="1">
            <TextBlock x:Name="EmptyText" Text="No recent activity"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="#757575" Visibility="Collapsed" />

            <ListView x:Name="ActivitiesList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="16,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Subject}" FontSize="13" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" TextWrapping="Wrap" />
                                <TextBlock Grid.Column="1" Text="{Binding TimeText}"
                                           FontSize="11" Foreground="#757575"
                                           VerticalAlignment="Top" Margin="8,0,0,0" />
                            </Grid>
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding AppLabel}"
                                           FontSize="11" Foreground="#0082c9" />
                                <TextBlock Text=" · " FontSize="11" Foreground="#757575" />
                                <TextBlock Text="{Binding ObjectName}"
                                           FontSize="11" Foreground="#757575"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <ProgressRing x:Name="LoadingRing" IsActive="True"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>
